import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// Fons principal càlid
  static const Color canvas = Color(0xFFFDF8F6);

  /// Targetes i contenidors
  static const Color surface = Color(0xFFFFFFFF);

  /// Salmó vibrant d'acció
  static const Color primary = Color(0xFFF2856D);

  /// Pressed / actiu
  static const Color primaryDark = Color(0xFFDE6D54);

  /// Rosa suau per a vores i tags
  static const Color accent = Color(0xFFF8B4A6);

  /// Text primari fosc càlid
  static const Color textMain = Color(0xFF2C2220);

  /// Subtítols i coordenades
  static const Color textMuted = Color(0xFF7D6E6B);
}
